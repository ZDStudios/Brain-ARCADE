services:
  - type: web
    name: brain-arcade-control
    runtime: node
    plan: free
    buildCommand: ""
    startCommand: node control-server/server.js
    envVars:
      - key: ADMIN_TOKEN
        sync: false
