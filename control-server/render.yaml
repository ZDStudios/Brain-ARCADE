services:
  - type: web
    name: brain-arcade-control
    runtime: node
    plan: free
    rootDir: control-server
    buildCommand: ""
    startCommand: node server.js
    envVars:
      - key: ADMIN_TOKEN
        sync: false
